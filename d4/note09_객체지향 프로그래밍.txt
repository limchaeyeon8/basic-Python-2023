# 객체지향 / 클래스
        붕어빵 틀 : 클래스
        붕어빵    : 객체
    변수, 함수를 알고 있어야 함

 
오브젝트(Object)    -    개체    -   객체(개체)
    ;  세상은 객체 ㅡ 명사(특징을 가지는; 변수)와
     동사(개체를 동작시키기 위한 움직임; 함수)의 집합으 ㅡ 로 구성됨

객체지향 프로그래밍
    : 현실세계의 모든 걸을 객체로 보고 프로그램화 하고자 하는 프로그래밍


* 클래스 : 데이터를 담는 속성과 행위를 담고 있는 함수(method)로 구성
    ; 객체를 만들어내는 틀

    [새로운 클래스 정의]
'''
    class 클래스명:
        속성변수 = 값               #클래스 안에서 사용하는 변수: 속성(객체특징 지칭)변수
    
    def 함수(self, [매개변수]):     # self 파라미터 : 속성(member)함수 // 필수 // 
        수행할 로직
        ~~~
        [return val]
'''

***** self는 필수   :   클래스 자기자신


# 생성자

class Person:                               <=== 괄호 없음 // 정의

클래스의 실체 : 인스턴스
실체를 만드는 것==생성  yeon = Person()     <=== 객체 생성 // 동사(명령) // 함수 호출

 매개변수(로컬/지역) != 속성(멤버)변수(글로벌/전역)



# 매직 메소드
단어 앞뒤로 언더바 두개씩 붙은 함수

20개 정도 좆재

파이썬에 내장함수와 동일한 기능을 개발자가 클래스에서 다시 정의하여 쓸 수 있도록 해주는 메소드
***** 재정의 == 오버라이딩; overriding

    __init_
    __new__
    __str__

# 클래스_2

***** 중복 == 오버로딩; overloading

인캡슐레이션

# 